services:
  browser-worker:
    build: .
    container_name: browser-use-worker
    restart: unless-stopped
    ports:
      - 8000:8000
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./main.py:/app/main.py
networks: {}
