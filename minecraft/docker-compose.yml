# Generated by setupmc.com on 2026-04-25T23:17:31.466Z

services:
  mc:
    image: itzg/minecraft-server:latest
    tty: true
    stdin_open: true
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      TYPE: "FABRIC"
      VERSION: "26.1"
      # MEMORY: "3072M"
      INIT_MEMORY: "1024M"
      MAX_MEMORY: "3072M"
      # flags to force Java to aggressively return unused RAM
      JVM_XX_OPTS: "-XX:+UseG1GC -XX:-ShrinkHeapInSteps -XX:MaxHeapFreeRatio=40 -XX:MinHeapFreeRatio=15"
      MAX_PLAYERS: "2"
      MOTD: "Vanilla Remaster 26.1"
      # USE_AIKAR_FLAGS: "true"
      TZ: "Europe/Vienna"
      DIFFICULTY: "3"
      SIMULATION_DISTANCE: "8"
      SEED: "${MC_SEED}"
      LEVEL: "Aetheria"
      SPAWN_PROTECTION: "0"
      MAX_TICK_TIME: "-1"
      REGION_FILE_COMPRESSION: "lz4"
      OPS: |-
        ${MC_USER}
      ENABLE_WHITELIST: "true"
      WHITELIST: |-
        ${MC_USER}
      MODRINTH_PROJECTS: |-
        ferrite-core
        lithium
        appleskin
        krypton
        clumps
        c2me-fabric
        scalablelux
        noisiumforked
        distanthorizons
      MODRINTH_DOWNLOAD_DEPENDENCIES: "required"
      MODRINTH_ALLOWED_VERSION_TYPE: "alpha"
      HIDE_ONLINE_PLAYERS: "true"
      ALLOW_FLIGHT: "true"
      ENABLE_ROLLING_LOGS: "true"
    volumes:
      - "/mnt/ssd/minecraft/data:/data"