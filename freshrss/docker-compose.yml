services:
  freshrss:
    image: lscr.io/linuxserver/freshrss:latest
    container_name: freshrss
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Vienna
    volumes:
      - /mnt/ssd/freshrss/config:/config
    ports:
      - 127.0.0.1:8081:80
    restart: unless-stopped